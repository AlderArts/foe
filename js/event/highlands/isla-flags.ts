export namespace IslaFlags {
    export enum Met {
        NotMet = 0,
        Met    = 1,
    }

    export enum Figure {
        Girly      = 0,
        Womanly    = 1,
        Voluptuous = 2,
    }

    export enum Talk { // Bitmask
        Spring = 1,
        Sex    = 2, // First time fucked
        SexVag = 4, // First time pitch vag
    }

    // Isla's spring
    export enum Bathing {
        Quick  = 0,
        Medium = 1,
        Long   = 2,
    }
}
